declare class LottieAnimation {
    animation: any;
    lottie: any;
    constructor();
    init({ lottie, container, url, on_loaded, on_complete, loop, autoplay, rendererSettings, initial_segment }: any): any;
}
export { LottieAnimation };
